#include<stdio.h>
#include<stdlib.h>
struct node{
    int i;
    struct node *next;
};
 
void main()
{ int num;
    struct node *head ;
    head=(struct node*)malloc(sizeof(struct node));
    
    printf("Enter how many nodes you going to create:");
    scanf("%d",num); 

    temp = head;

    while (int i=0<=num ){
        tep-=(struct node*)malloc(sizeof(struct node));
        temp->i=
    }

}